apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: relay-ipv6-ingress
  namespace: {{ .Values.namespace | quote }}
  annotations:
    kubernetes.io/ingress.global-static-ip-name: {{ .Values.cardanoNode.relayIpv6 | quote }}
spec:
  backend:
    serviceName: relay-backend
    servicePort: 3001